---
- name: "Skel {{ skel }} directory present"
  file:
    path: "/usr/local/etc/skel.d/{{ skel }}"
    state: directory
  tags:
    - skel

- name: "Skel {{ skel }} files in place"
  copy:
    src: "files/skel.d/{{ skel }}/{{ item }}"
    dest: "/usr/local/etc/skel.d/{{ skel }}/{{ item }}"
    owner: root
    group: root
    mode: 0600
  loop:
    - .bash_logout
    - .bashrc
    - .profile
    - .vimrc
  tags:
    - skel

- name: "Skel {{ skel }} private directories present"
  file:
    path: "/usr/local/etc/skel.d/{{ skel }}/{{ item }}"
    state: directory
    owner: root
    group: root
    mode: 0700
  with_items:
    - .ansible
    - .cache
    - .composer
    - .config
    - .drush
    - .git
    - .gnupg
    - .local
    - .mysql
    - .mutt
    - .npm
    - .ssh
    - .vim
    - .wp-cli
    - .yarn
    - backups
    - bin
    - private
    - tmp
  tags:
    - skel

- name: "Skel {{ skel }} files touched"
  file:
    path: "/usr/local/etc/skel.d/{{ skel }}/{{ item }}"
    state: touch
    owner: root
    group: root
    mode: 0600
    modification_time: preserve
    access_time: preserve
  with_items:
    - .bash_history
    - .mysql/history
    - .selected_editor
    - .yarnrc
  tags:
    - skel

- name: "Skel {{ skel }} WP-CLI config in place"
  copy:
    src: "files/skel.d/{{ skel }}/.wp-cli/config.yml"
    dest: "/usr/local/etc/skel.d/{{ skel }}/.wp-cli/config.yml"
    owner: root
    group: root
    mode: 0600
  tags:
    - skel
...
